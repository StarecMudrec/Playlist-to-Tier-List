services:
  web:
    build: .
    ports:
      # Bind only to localhost so the app isn't directly reachable from the Internet.
      - "127.0.0.1:5000:5000"
    environment:
      - PORT=5000
      # Включить debug при необходимости:
      # - FLASK_DEBUG=1

